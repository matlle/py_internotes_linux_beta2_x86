# -*- mode: python -*-
a = Analysis(['main.py'],
             pathex=['C:\\Users\\lartuak\\Desktop\\progs\\matlle\\py\\Py_internotes_win_x86'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='InterNotes.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='InterNotes')
